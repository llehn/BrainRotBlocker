# OS
Thumbs.db
Desktop.ini
.DS_Store

# Editors / IDEs
.vs/
.vscode/
.idea/
*.user
*.suo

# Build output (language TBD)
bin/
obj/
build/
dist/
out/
target/

# Logs and local runtime state
*.log
*.tmp

# Local enforcement / runtime state (never commit budget state or secrets)
*.local.json
local-state/

# Published single-file distributable and scratch screenshots
publish/
.brd-*

# Update signing private key (set it as the BRD_UPDATE_PRIVATE_KEY GitHub secret;
# never commit it). The matching public key is embedded in source.
tools/release/private-key.b64
